# 《这个神坛不太灵》Demo · 素材接入说明(Vibecoding 版)

> 这份说明对应当前的 2 神 demo(`这个神坛不太灵-demo.html`)。
> demo 现在能完整跑通,但里面的神像、表情包都是**临时占位美术**(代码画的)。
> 这份文档告诉你:素材放哪个文件夹、怎么命名、改 HTML 哪一行接进去。
> (16 神的完整规划在旧文档《神坛项目建设文档》,本阶段以这份为准。)

---

## 1. 当前 demo 是什么

- **一个 HTML 文件**,打开即跑,不依赖任何外部素材。
- 信息流 4 屏:考研视频 → **求上岸神** → 熬夜脱发视频 → **不掉头发神**。
- 全流程已通:祈愿 → 抽签 → 表情包 → 收下/分享 → 灵验回访。
- 待替换的占位:神像(发光法印)、表情包(代码画的脸)、视频(CSS 场景)。

**两位神的内部 ID(素材命名要用到):**

| 神 | ID | 字 |
|---|---|---|
| 求上岸神 | `anside` | 岸 |
| 不掉头发神 | `toufa` | 发 |

---

## 2. 第一步:建好文件夹

新建一个项目文件夹,把 demo 放进去,再建一个 `assets/` 装素材:

```
这个神坛不太灵/                      ← 自己新建的项目文件夹
├── 这个神坛不太灵-demo.html          ← 现在这个 demo,放进来
└── assets/                          ← 素材总文件夹,自己新建
    ├── gods/                        神像(2 张)
    │   ├── anside.png               求上岸神
    │   └── toufa.png                不掉头发神
    ├── stickers/                    表情包(6 情绪 × 2 神 = 12 张)
    │   ├── anside_kuangxi.png        …(6 张,见下表)
    │   └── toufa_kuangxi.png         …(6 张)
    ├── videos/                      信息流视频(可选,2 个)
    │   ├── video1.mp4
    │   └── video2.mp4
    └── audio/                       音频(可选)
        ├── ambient.mp3
        └── sign.mp3
```

> ⚠️ 文件名**只用英文/数字/下划线,别用空格和中文**,严格照下面的命名,否则代码找不到。

**表情包的 6 个情绪 → 文件名后缀**(每位神都要出全 6 张):

| 情绪 | 文件名后缀 | 例 |
|---|---|---|
| 狂喜 | `kuangxi` | `anside_kuangxi.png` |
| 淡定 | `danding` | `toufa_danding.png` |
| 认命 | `renming` | … |
| 摆烂 | `bailan` | … |
| 阴阳 | `yinyang` | … |
| 破防 | `pofang` | … |

---

## 3. 素材清单(总览)

| 素材 | 数量 | 放进文件夹 | 命名规则 | 优先级 |
|---|---|---|---|---|
| 神像 | 2 | `assets/gods/` | `{ID}.png` | ★★★ 最该换 |
| 表情包 | 12 | `assets/stickers/` | `{ID}_{情绪后缀}.png` | ★★★ 传播核心 |
| 音频 | 2 | `assets/audio/` | `ambient.mp3` `sign.mp3` | ★ 锦上添花 |
| 视频 | 2 | `assets/videos/` | `video1.mp4` `video2.mp4` | ☆ 可选(CSS 场景已够用) |

> 神像、表情包要**透明背景 PNG**;表情包**图里不要带文字**(判词由代码叠上去)。

---

## 4. 怎么把素材接进 demo

在 `这个神坛不太灵-demo.html` 里按下面改。每一类都标了「搜什么 / 改成什么」。

### 4.1 神像(把发光法印换成真神像)

HTML 里有**两处** `<div class="seal">`。各替换:

- 求上岸神(`<div class="seal"><div class="glyph">岸</div></div>`)→
  `<img class="god-img" src="assets/gods/anside.png" alt="求上岸神">`
- 不掉头发神(`<div class="seal"><div class="glyph">发</div></div>`)→
  `<img class="god-img" src="assets/gods/toufa.png" alt="不掉头发神">`

再到 `<style>` 里(`.seal` 规则附近)加一条:

```css
.god-img{width:142px;height:auto;position:relative;z-index:3;
  filter:drop-shadow(0 0 26px rgba(230,196,131,.55));
  animation:breathe 4.4s ease-in-out infinite;}
```

> 保留 `.niche` 和 `.mandala`——那是神像背后的发光神龛和曼陀罗,透明背景的神像叠在前面正好。
> (祈愿动画里的 `.pray-seal` 可以不动,留着发光法印也好看;想换也照上面同理。)

### 4.2 表情包(把代码画的脸换成真表情包)

找到 JS 里的 `function stickerSVG(id,mood,title){ ... }`,**整个函数**替换为:

```js
function stickerSVG(id,mood,title){
  const m={狂喜:'kuangxi',淡定:'danding',认命:'renming',摆烂:'bailan',阴阳:'yinyang',破防:'pofang'};
  const file='assets/stickers/'+id+'_'+(m[mood]||'danding')+'.png';
  return `<div class="sticker-real">
    <img src="${file}" onerror="this.src='assets/stickers/'+'${id}'+'_danding.png'" alt="">
    <div class="sticker-cap">${title}</div>
  </div>`;
}
```

再到 `<style>` 里加:

```css
.sticker-real{width:100%;height:100%;position:relative;}
.sticker-real img{width:100%;height:100%;object-fit:cover;display:block;}
.sticker-cap{position:absolute;left:0;right:0;bottom:16px;text-align:center;
  font-family:var(--f-brush);font-size:30px;font-weight:700;color:#2c1a0c;
  text-shadow:2px 2px 0 #fff,-2px -2px 0 #fff,2px -2px 0 #fff,-2px 2px 0 #fff,
    0 0 4px #fff;}
```

> 判词(`title`)由代码叠在表情包下方,所以表情包**图本身别带字**。
> 如果你们出的表情包图已经自带文字,就把上面 `<div class="sticker-cap">${title}</div>` 删掉,只留 `<img>`。

### 4.3 音频(可选)

demo 现在没声音。想加:在 `</body>` 前面插入:

```html
<audio id="bgm" src="assets/audio/ambient.mp3" loop></audio>
<audio id="sfx" src="assets/audio/sign.mp3"></audio>
```

然后在 JS 里加两行:
- `function pray(id){` 的第一行后面加 → `document.getElementById('bgm').play().catch(()=>{});`
  (环境音在用户点「祈愿」时启动,符合浏览器自动播放限制)
- `function renderSign(id,s,isAI,wish){` 里、`st.signNo++;` 那行后面加 →
  `document.getElementById('sfx').play().catch(()=>{});`(出签时响一声)

### 4.4 视频(可选,CSS 场景已经能用)

想把信息流里的视频换成真视频:把 `<div class="vid-scene scene-study"> … </div>`
**整段**换成:

```html
<video class="vid-scene" src="assets/videos/video1.mp4" autoplay loop muted playsinline></video>
```

熬夜那条同理,`scene-night` 换成 `assets/videos/video2.mp4`。
CSS 里给 `.vid-scene` 补一句 `object-fit:cover;`。也可以用一张图代替(`<img class="vid-scene" src="...">`)。

---

## 5. 素材生成提示词

### 神像 ×2(Seedream 5.0)

**通用风格句(两张都用,一字不改)**:
```
Q版国潮神像,三头身,[角色],盘腿端坐,手持[法器],[神情];
身后金色曼陀罗背光,深色神龛,国潮插画厚涂质感;
配色暖金+朱砂红+少量赛博青色发光点缀;
正方形构图,角色居中完整,透明背景便于抠图;画面不要任何文字、不要水印。
```
- **求上岸神**:`[角色]`=头戴学士帽的年轻神仙;`[法器]`=一卷录取通知书;`[神情]`=笃定温柔、给人希望。
- **不掉头发神**:`[角色]`=一头浓密飘逸长发的神仙(头发即法力);`[法器]`=一把木梳;`[神情]`=「发量保住了」的欣慰、强行淡定。

### 表情包 ×12(Seedream 5.0)

**模板**(每神 × 6 情绪):
```
Q版[神名]半身表情包,[情绪动作],与神像同一国潮厚涂风格,夸张可爱有梗感;
透明背景 PNG,正方形,角色居中;画面不要任何文字。
```
| 情绪 | 动作 |
|---|---|
| 狂喜 | 双手举起、金光四射、笑开花 |
| 淡定 | 微眯眼、稳如老狗、比个手势 |
| 认命 | 摊手、叹气、眼神空洞但平静 |
| 摆烂 | 整个瘫倒、葛优躺、生无可恋 |
| 阴阳 | 似笑非笑、挑眉、皮笑肉不笑 |
| 破防 | 瞳孔地震、表情裂开、夸张震惊 |

### 音频 ×2(Suno)

- `ambient.mp3`:`中式寺庙环境氛围,木鱼轻敲、风铃、远处低钟,平静空灵,带一丝赛博电子点缀,无人声,可无缝循环,约60秒`
- `sign.mp3`:`短促明亮的民乐音效,铜铃+古筝拨弦,2~3秒,有"灵签出现"的喜悦感`

### 视频 ×2(可选 · Seedance 2.0)

- `video1.mp4`:`凌晨的自习室,一盏台灯、一摞书、窗外夜色,氛围安静努力,竖屏,可循环,无人物特写`
- `video2.mp4`:`深夜卧室,手机屏幕的冷光,凌乱的床,氛围疲惫,竖屏,可循环`

---

## 6. 验收清单

接完素材后逐项检查:

- ☐ 两位神都显示成**真神像**(不是发光法印),稳稳坐在神龛里。
- ☐ 抽签后表情包是**真图**,判词文字清晰叠在上面。
- ☐ 多抽几次,6 种情绪的表情包都能正确出现、不出现裂图。
- ☐ AI 现批(打字许愿)返回的签,表情包也能正常显示。
- ☐ (若加了音频)点祈愿有环境音,出签有音效。
- ☐ 整个信息流照常能上下滑,流程不报错。

---

## 7. 小贴士

- 素材文件名严格照第 2 章命名,**别用空格和中文**。
- `assets/` 文件夹必须和 HTML 放在**同一层**;HTML 改名没关系,路径是相对它自己的。
- 神像、表情包用**透明背景 PNG**。
- 加了外部素材后,如果双击 HTML 图片不显示,用一个本地服务器打开即可
  (例如终端 `python -m http.server`,或任意静态服务器),部署后也正常。
- 想加第 3、第 4 个神:在 JS 的 `DEITIES` 里照样复制一份、在信息流里再加一组「视频+神」卡片即可——需要时我帮你们改。

---

*素材出齐 → 照第 4 章接进去 → 对第 6 章验收。完。*
